// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: deputy/scan/v1/service.proto

package scanv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/temporalio/deputy/gen/deputy/scan/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ScanServiceName is the fully-qualified name of the ScanService service.
	ScanServiceName = "deputy.scan.v1.ScanService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ScanServiceScanProcedure is the fully-qualified name of the ScanService's Scan RPC.
	ScanServiceScanProcedure = "/deputy.scan.v1.ScanService/Scan"
	// ScanServiceStreamScanProcedure is the fully-qualified name of the ScanService's StreamScan RPC.
	ScanServiceStreamScanProcedure = "/deputy.scan.v1.ScanService/StreamScan"
)

// ScanServiceClient is a client for the deputy.scan.v1.ScanService service.
type ScanServiceClient interface {
	// Scan performs a vulnerability scan on a target.
	// The target can be a directory, Git repository, container image, SBOM, etc.
	// See service-level documentation for mode-specific security constraints.
	Scan(context.Context, *connect.Request[v1.ScanRequest]) (*connect.Response[v1.ScanResponse], error)
	// StreamScan performs a vulnerability scan with streaming progress updates.
	// Useful for long-running scans where clients want real-time feedback.
	// See service-level documentation for mode-specific security constraints.
	StreamScan(context.Context, *connect.Request[v1.StreamScanRequest]) (*connect.ServerStreamForClient[v1.ScanProgress], error)
}

// NewScanServiceClient constructs a client for the deputy.scan.v1.ScanService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewScanServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ScanServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	scanServiceMethods := v1.File_deputy_scan_v1_service_proto.Services().ByName("ScanService").Methods()
	return &scanServiceClient{
		scan: connect.NewClient[v1.ScanRequest, v1.ScanResponse](
			httpClient,
			baseURL+ScanServiceScanProcedure,
			connect.WithSchema(scanServiceMethods.ByName("Scan")),
			connect.WithClientOptions(opts...),
		),
		streamScan: connect.NewClient[v1.StreamScanRequest, v1.ScanProgress](
			httpClient,
			baseURL+ScanServiceStreamScanProcedure,
			connect.WithSchema(scanServiceMethods.ByName("StreamScan")),
			connect.WithClientOptions(opts...),
		),
	}
}

// scanServiceClient implements ScanServiceClient.
type scanServiceClient struct {
	scan       *connect.Client[v1.ScanRequest, v1.ScanResponse]
	streamScan *connect.Client[v1.StreamScanRequest, v1.ScanProgress]
}

// Scan calls deputy.scan.v1.ScanService.Scan.
func (c *scanServiceClient) Scan(ctx context.Context, req *connect.Request[v1.ScanRequest]) (*connect.Response[v1.ScanResponse], error) {
	return c.scan.CallUnary(ctx, req)
}

// StreamScan calls deputy.scan.v1.ScanService.StreamScan.
func (c *scanServiceClient) StreamScan(ctx context.Context, req *connect.Request[v1.StreamScanRequest]) (*connect.ServerStreamForClient[v1.ScanProgress], error) {
	return c.streamScan.CallServerStream(ctx, req)
}

// ScanServiceHandler is an implementation of the deputy.scan.v1.ScanService service.
type ScanServiceHandler interface {
	// Scan performs a vulnerability scan on a target.
	// The target can be a directory, Git repository, container image, SBOM, etc.
	// See service-level documentation for mode-specific security constraints.
	Scan(context.Context, *connect.Request[v1.ScanRequest]) (*connect.Response[v1.ScanResponse], error)
	// StreamScan performs a vulnerability scan with streaming progress updates.
	// Useful for long-running scans where clients want real-time feedback.
	// See service-level documentation for mode-specific security constraints.
	StreamScan(context.Context, *connect.Request[v1.StreamScanRequest], *connect.ServerStream[v1.ScanProgress]) error
}

// NewScanServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewScanServiceHandler(svc ScanServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	scanServiceMethods := v1.File_deputy_scan_v1_service_proto.Services().ByName("ScanService").Methods()
	scanServiceScanHandler := connect.NewUnaryHandler(
		ScanServiceScanProcedure,
		svc.Scan,
		connect.WithSchema(scanServiceMethods.ByName("Scan")),
		connect.WithHandlerOptions(opts...),
	)
	scanServiceStreamScanHandler := connect.NewServerStreamHandler(
		ScanServiceStreamScanProcedure,
		svc.StreamScan,
		connect.WithSchema(scanServiceMethods.ByName("StreamScan")),
		connect.WithHandlerOptions(opts...),
	)
	return "/deputy.scan.v1.ScanService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ScanServiceScanProcedure:
			scanServiceScanHandler.ServeHTTP(w, r)
		case ScanServiceStreamScanProcedure:
			scanServiceStreamScanHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedScanServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedScanServiceHandler struct{}

func (UnimplementedScanServiceHandler) Scan(context.Context, *connect.Request[v1.ScanRequest]) (*connect.Response[v1.ScanResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("deputy.scan.v1.ScanService.Scan is not implemented"))
}

func (UnimplementedScanServiceHandler) StreamScan(context.Context, *connect.Request[v1.StreamScanRequest], *connect.ServerStream[v1.ScanProgress]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("deputy.scan.v1.ScanService.StreamScan is not implemented"))
}
