// Package ui centralizes terminal presentation styles (colors, emphasis) so
// that higher level reporting code can remain logic‑focused and stylistically
// consistent. Styles are expressed via lipgloss Style values and grouped by
// semantic purpose (added/removed packages, severity coloring, metadata, etc.).
package ui
